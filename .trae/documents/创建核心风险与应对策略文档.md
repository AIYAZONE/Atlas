明白了，你需要两份截然不同但互补的文档：

1.  **风险控制文档**：侧重“坑”在哪里，如何避坑（GA断连、库存超卖、Checkout割裂等）。
2.  **重难点清单文档**：侧重“硬骨头”是哪些，开发时要啃哪里（编辑器拖拽、Schema设计、SSG构建流等）。

我将为你创建两份新文档：

### 文档 1：`docs/10_核心风险与应对策略.md` (Risk Register)
这份文档已经在刚才的计划中，涵盖 GA4 连续性、实时性、App 兼容性等 10+ 个风险点。

### 文档 2：`docs/11_开发重难点清单.md` (Technical Challenges)
这份文档专门梳理 **项目创建及开发过程中的“硬骨头”**，帮助团队在排期和分配任务时有的放矢。

**`docs/11_开发重难点清单.md` 内容大纲：**

#### A. 架构基建重难点
1.  **Monorepo 依赖管理**：确保 Schema 包在 Builder 和 Renderer 间共享类型的配置复杂度。
2.  **Supabase RLS 策略**：如何设计行级安全策略，既允许未登录用户读页面，又只允许管理员写页面。
3.  **SSG 构建流水线**：如何实现增量构建 (ISR)？全量构建时如何处理 Shopify API 限流 (429)？

#### B. 可视化编辑器 (Builder) 重难点
1.  **跨 Frame 通信**：Builder（父）与 Preview（子）的双向通信、样式隔离与点击劫持。
2.  **Schema 动态表单**：如何根据 JSON Schema 自动生成右侧属性面板的 Input/Select/ColorPicker 组件，并处理联动逻辑。
3.  **拖拽与排序**：在 iframe 内部实现平滑的组件拖拽，并实时更新 JSON 树。

#### C. 电商业务重难点
1.  **购物车状态机**：本地 Cache、服务端 Cart、UI 状态的三方同步与冲突解决。
2.  **用户账户体系**：Headless 站登录态与 Shopify Multipass 免登跳转的打通。
3.  **App 功能复刻**：Review/Loyalty 等第三方 App 的前端 UI 重写。

---

### 执行计划
我将一次性为你创建这两份文档。确认后，你将拥有完整的“避坑指南”和“攻坚指南”。
